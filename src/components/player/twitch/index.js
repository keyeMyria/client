import React from 'react';

let PlayerTwitch = () => (
  <div>
    Stream Twitch
  </div>
);

// export default connect(
//   state => ({
//     playData: state.room.playData
//   })
// )(RoomPlayerTwitch);